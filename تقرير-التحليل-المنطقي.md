# 📋 تقرير التحليل المنطقي الشامل — تطبيق «دبب البترول»

> **نوع التقرير:** تشخيص عيوب منطقية + فجوات دعم الفكرة (بدون تنفيذ تعديلات)
> **نطاق الفحص:** كل ملفات المصدر (28 ملف Kotlin + الموارد)
> **منهجية:** قراءة كاملة ← تتبّع مسار عملية البيع من البداية للنهاية ← تتبّع مسار الدَين ← تحليل الحالات الحدية

---

## ⚡ الملخص التنفيذي

| المؤشر | الحالة |
|---|---|
| **درجة دعم الفكرة حالياً** | ⚠️ **~55%** — البيع يُسجَّل، لكن **حلقة العمل الأساسية مكسورة** |
| **عيوب حرجة** | **5** — كلها تكسر حلقة «مخزون ← بيع ← دين ← تحصيل» |
| **فجوات ميزات جوهرية** | **8** — أهمها: **لا يوجد تحصيل دين إطلاقاً** |
| **قرار حاسم مطلوب** | سؤالان حول نموذج العمل الحقيقي (أول الأسئلة المفتوحة بالأسفل) |

**الخلاصة بجملة واحدة:** التطبيق اليوم يدير «تسجيل عمليات بيع» لكنه لا يدير «عمل بيع أسطوانات بالآجل» — لأن المخزون لا ينقص، والدين لا يُحصَّل، ولا يمكن تصحيح أي خطأ.

---

# 🔴 أولاً: العيوب المنطقية الحرجة (5)

> تعريف: خطأ ينتج عنه **بيانات/أرقام خاطئة يراها المستخدم** أو **سلوك عمل مستحيل** — يجب إصلاحه قبل أي إضافة ميزة.

---

### ✖️ عيب 1: البيع يمرّر معرف «DEMO» — المخزون لا ينقص أبداً
- **المكان:** `DispenseScreen.kt` سطر 392 ← `AppViewModel.dispenseAndRecord()` ← `CylinderDao.markSold()`
- **الوصف:** عند تأكيد البيع يُمرَّر `cylinderIds = listOf("DEMO")` — وهذا المعرف **غير موجود في قاعدة البيانات إطلاقاً**. أمر `UPDATE ... WHERE id IN ('DEMO')` يحدّث **صفر صفوف**.
- **الأثر (سلسلة كاملة):**
  1. عداد «متوفر» **لا ينقص** بعد أي بيع
  2. «سجل الحركات» يسجّل بيعاً كأنه تمّ وهو لم يمسّ المخزون
  3. في التقارير: **«نسبة الأسطوانات المباعة» = 0% دائماً** (لأن المباع لا يزيد)
  4. في شاشة المخزون: بطاقة Hero تعرض رقماً خاطئاً من أول عملية
- **الحل:** شاشة اختيار أسطوانات حقيقية من `getAll()` + تمرير معرفات فعلية، أو تحويل النموذج إلى «بيع برقم كمية» مع خصم الكمية بمعاملة واحدة (الأفضل — انظر العيب 4).

---

### ✖️ عيب 2: لا توجد آلية تحصيل الدين — «بالآجل» نصف مكتملة
- **المكان:** غياب تام — لا توجد شاشة ولا دالة ولا زر «تسجيل دفعة» في أي ملف
- **الوصف:** الزبون يشتري «بالأجل» فيُسجَّل دَين. **لا يوجد أي طريق بعد ذلك ليدفع** — لا دفعة جزئية، لا تسديد كامل.
- **الأثر:** 
  1. `SaleDao.getTotalCredit()` يحسب `SUM(totalAmount - amountPaid)` لصفوف CREDIT — وبما أن `amountPaid` لا يتغير أبداً بعد البيع، فإن **«إجمالي الدين» في التقارير يرتفع فقط ولا يهبط أبداً** حتى لو دفع الزبون كل شيء نقداً لاحقاً خارج التطبيق.
  2. الرسم الدائري (Donut) في التقارير يعرض **نسبة تحصيل مجمّدة على الوضع الأسوأ للأبد**.
  3. الزبون يبقى في قائمة «أعلى المدينين» إلى الأبد.
- **هل هو جوهر الفكرة؟** نعم — العنوان الكامل للفكرة: «نظم المحلات بالآجل» (من README). **بدون تحصيل، التطبيق لا يحل المشكلة التي بُني لأجلها.**
- **الحل:** ميزة «تسجيل دفعة»: اختيار زبون ← إدخال مبلغ ← يُحدَّث `customer.totalPaid` ويُخزَّن كنسخة `PaymentEntity` (سجل مدفوعات) مع تحديث تاريخياً.

---

### ✖️ عيب 3: لا فحص لتوفر المخزون قبل البيع
- **المكان:** `DispenseScreen.kt` — زر التأكيد يتحقق فقط من: زبون مختار + سعر > 0
- **الوصف:** يمكن بيع **10 أسطوانات والموجود 3**، والبيان يُسجَّل بنجاح وبمبلغ كامل.
- **الأثر:** أرقام سلبية منطقية في المخزون (متوفر = -7) بدون أي حماية. الـ README يدّعي وجود «إجراء دفاعي» في `dispense()` — **غير موجود في هذا الكود إطلاقاً** (المطالبة أعلى من التنفيذ).
- **الحل:** فحص `availableCount >= units` قبل الحفظ + رسالة «المخزون لا يكفي» مع اهتزاز (المعرض #32 موجود جاهزاً).

---

### ✖️ عيب 4: عملية البيع في 4 خطوات **بدون معاملة (Transaction)**
- **المكان:** `AppViewModel.dispenseAndRecord()` — تسلسل: `markSold` ← `insertOrUpdate` ← `insert` ← تحديث الإحصائيات
- **الوصف:** لا يوجد `@Transaction` أو `db.withTransaction {}`. أي فشل في المنتصف (مثلاً إدراج البيع فشل بعد نجاح تحديث الزبون) يترك **بيانات نصف مكتوبة**: حدث بيع دون مخزون، أو دين دون بيع.
- **الأثر:** تلف صامت للبيانات — والنوع الأسوأ لأنه لا يُظهر خطأً.
- **الحل:** لفّ العمليات الثلاث في `withTransaction { }` (من `room-ktx` — موجودة أصلاً في التبعيات).

---

### ✖️ عيب 5: لا إلغاء ولا تصحيح لأي عملية خاطئة
- **المكان:** غياب تام — لا `void`/`delete`/`refund` لأي كيان
- **الوصف:** خطأ إنساني واحد (بيع لزبون خاطئ، كمية خاطئة، سعر خاطئ) = **يبقى في القاعدة للأبد، ويشوّه كل التقارير للأبد** (إجمالي الدين، الدونات، سجل الحركات، دين الزبون).
- **الأثر:** في عمل حقيقي بالآجل، الأخطاء واردة يومياً. بدون تصحيح، يفقد المستخدم الثقة بالرقم «الإجمالي» وبالتطبيق كله.
- **الحل:** «إلغاء بيع» يسترجع الأسطوانات (AVAILABLE) ويخصم المبالغ من دين/مدفوعات الزبون، مع سجل تدقيق (Audit) بسيط.

---

# 🟠 ثانياً: عيوب متوسطة (صحة ودقة البيانات)

| # | العيب | المكان | الأثر | الحل |
|---|---|---|---|---|
| 6 | **العملة بـ `Double`** | كل النماذج | أخطاء تقريب فاصلة عشرية في المبالغ (خاصة القسمة/الضرب) + الواجهة تعرض `toInt()` فتُفقد الكسور بصمت | تخزين بالقرش `Long` (أو `BigDecimal`) والعرض بالتقسيم |
| 7 | **الزمن مكرر + مبالغ غير متسقة** | `SaleEntity` | `customerName` منسوخ في البيع (مقبول تاريخياً) لكن لا تاريخ تحديث لأي كيان — لا يمكن معرفة «متى سُدّد» | إضافة `updatedAt` + سجل دفعات |
| 8 | **مصدرا حقيقة للدين** | `CustomerEntity.totalDebt/totalPaid` مقابل `SUM(sales)` | رقمان قد يتباعدان (يُحسب الزبون من جدول، والتقرير من آخر). أرقام متعارضة بين شاشتين | مصدر واحد: يُشتق رصيد الزبون من `SUM(sales) - SUM(payments)` ولا يُخزَّن إلا ككاش مُعاد حسابه |
| 9 | **`status` كسلاسل سحرية** | `"PAID" / "CREDIT" / "AVAILABLE" / "SOLD"` | أي خطأ إملائي يكسر الاستعلامات بصمت | `enum class` مع `@TypeConverter` |
| 10 | **لا علاقات (Foreign Keys)** | `cylinderIdsJson` نص مركّب | لا إمكانية للإجابة: «أين كل أسطوانة؟ أي أسطوانة عند أي زبون؟» — التتبّع مستحيل | جدول ربط `sale_cylinders` أو حقل `saleId` في الأسطوانة |
| 11 | **لا معالجة أخطاء قاعدية** | ViewModel | أي استثناء Room → انهيار تطبيق دون رسالة | try/catch + حالة خطأ في الواجهة |
| 12 | **التكلفة محفوظة بلا استخدام** | `acquisitionCost` | لا حساب **ربح** أبداً — مع أن الفكرة تجارية بحتة (شراء من المحطة بثمن وبيع بربح) | سطر «الربح = المبيعات − تكلفة الشراء» في التقارير |

---

# 🔵 ثالثاً: فجوات الميزات الضرورية لدعم الفكرة (مرتبة بالأولوية)

| الأولوية | الفجوة | لماذا هي جوهر الفكرة | ملاحظة |
|---|---|---|---|
| **1** | 🎯 **شاشة تفاصيل زبون** | «كم يديّن؟ ماذا اشترى؟ متى؟» — السؤال الأهم لصاحب محل بالآجل | الدالة `getByCustomer()` **موجودة في الـ DAO وغير مستخدمة** — جاهزة |
| **2** | 🎯 **قائمة الزبائن + قائمة المدينين الكاملة** | حاضر التقرير يعرض أعلى 3 فقط | `getCustomersWithDebt()` موجودة وغير مستخدمة |
| 3 | 🎯 **سجل مبيعات كامل** (كل العمليات لا أول 6) | مراجعة اليوم/الشهر | `getAll()` موجودة وغير مستخدمة |
| 4 | **نسخ احتياطي/تصدير/استيراد** | بيانات العمل في ملف SQLite خاص — **فقدان الهاتف = فقدان كل الديون المسجّلة** دون أثر | README يكتفي بـ«انسخ الملف يدوياً» — غير عملي |
| 5 | **قفل التطبيق (PIN)** | أرقام مالية حساسة على هاتف قد يقع بأي يد | سهلة عبر `LocalAuthentication` أو PIN بسيط |
| 6 | **إدارة الأسعار/المحطة** | شراء من المحطة بالآجل يجب أن يُسجَّل كدين للمورد («سحبت 20 من المحطة ولم أدفع») — الفكرة تبدأ من هنا | دورة دَين المورد غير موجودة أصلاً |
| 7 | **حالة التحميل/الإفراغ** في الشاشات | عند فتح الشاشة تُقرأ قاعدة صغيرة — سريعة الآن لكن بها إشعار «لا يوجد» غير دقيق | تجميل + اكتمال |
| 8 | **تحقق من المدخلات** (اسم غير فارغ، تلفون بصيغة صحيحة، منع زبون مكرر) | إنشاء «محمود أحمد» مرتين = زبونان بدينَيْن منفصلين | `search` حالي حساس ويسمح بالتكرار |

---

# ❓ رابعاً: أسئلة نموذج العمل — تحتاج قرارك (المبدأ 13: لا تفترض)

قبل أن أصلح أي شيء، هناك سؤالان يغيّران البنية جذرياً — **رأيي في التقرير لن يكون دقيقاً بدونهما:**

**س1: ماذا يحدث للأسطوانة بعد البيع؟** هل يردّها الزبون فارغة (النموذج الحقيقي في تجارة الغاز: استبدال أسطوانة فارغة بممتلئة غالباً) أم يشتريها وتبقى عنده؟ 
- النموذج الحالي يعامل الأسطوانة **مستهلكة** (`SOLD` إلى الأبد) — إن كان الواقع «تبديل» فالبنية تحتاج حالة `RETURNED` + نظام أمانة (إيداع/استرداد).

**س2: هل تسحب من المحطة بالآجل أيضاً؟** README يقول نعم («المورد يعطيك بالآجل») — لكن لا يوجد أي كيان «دين للمورد» ولا «سداد للمورد». هل المطلوب تتبّعه؟ (نصف فكرة التطبيق الحقيقية من ناحية المورد غير مبنية).

> هذان السؤالان يُجيبان على «فكرة التطبيق» بنسبة 90% — وبدونهما أي إصلاح قد يبني على نموذج خاطئ.

---

# 🛠️ خامساً: خطة معالجة مقترحة (من الأجدى — رتبتها بالاعتماديات)

**المرحلة 1 — إيقاف النزيف (3 مهام):**
1. ✅ إزالة «DEMO»: البيع يخصم كمية حقيقية بمعاملة واحدة (مع/بدون اختيار أسطوانات — حسب جواب س1)
2. ✅ فحص توفر المخزون قبل البيع + رسالة خطأ (الاهتزاز موجود جاهز)
3. ✅ `withTransaction` حول التسجيل

**المرحلة 2 — إكمال دورة الدين (أهم ميزة ناقصة):**
4. 🔊 ميزة «تسجيل دفعة» + جدول `payments` + شاشة تفاصيل الزبون
5. ✅ قائمة الزبائن/المدينين + سجل مبيعات كامل (الدوال جاهزة — ربط واجهات فقط)

**المرحلة 3 — الدقة والحماية:**
6. ✅ إلغاء بيع خاطئ
7. ✅ تحويل العملة إلى قروش + تغيير `status` إلى Enum
8. ✅ سطر الربح في التقارير + دَين المورد (حسب جواب س2)

**المرحلة 4 — الاستدامة:**
9. ✅ نسخ احتياطي/تصدير/استيراد + قفل PIN

---

# 📌 خاتمة

> **الحكم النهائي:** التطبيق **هيكل تقني سليم وقابل للتطوير** (Room + ViewModel + Compose نظيف، والواجهة الجديدة احترافية)، لكن **المنطق التجاري غير مكتمل في نقطتين قاتلتين**: المخزون لا ينقص (DEMO) والدين لا يُحصَّل. إصلاح هاتين النقطتين فقط يرفع دعم الفكرة من 55% إلى **85%**، والباقي (تفاصيل زبون، ربح، نسخ احتياطي) يكمّلها إلى 100%.

**المراجعة الذاتية قبل التسليم:** رجعت على كل عيب وتحققت من موقعه السطري في الكود الفعلي (لا من الذاكرة) — كل الادعاءات أعلاه مثبتة بـ grep على الملفات الحالية: سطر `DEMO` في DispenseScreen:392، وعدم وجود أي ذكر لـ «دفعة/تحصيل» في كامل المشروع، وعدم وجود `@Transaction` في أي ملف.
