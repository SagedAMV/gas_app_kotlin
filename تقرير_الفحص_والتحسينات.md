# 📋 تقرير الفحص وإعادة البناء — جلسة 2026

> تنفيذ لملف `تعليمات.md` وفق إطار `تفكير.md` (فهم ← تقسيم ← تحليل ← تنفيذ ← فحص ← فحص عكسي).
> التطبيق شخصي وليس للنشر.

---

## 1️⃣ فهم منطق التطبيق (قراءة معمّقة للكود)

التطبيق نظام إدارة **تجارة أسطوانات غاز** (اليمن — الريال الصحيح بلا كسور):

| الطبقة | الملفات | الخلاصة |
|---|---|---|
| الكيانات | `model/*Entity.kt` | زبائن، أسطوانات (متوفر/مباع)، مبيعات، تحصيلات، سحوبات المحطة، تسديداتها — كل المبالغ `Long` |
| البيانات | `data/local/*` | Room v5 بمفاتيح أجنبية `RESTRICT` على المبيعات/التحصيلات + قيود `CHECK` + هجرات صريحة بلا `fallbackToDestructiveMigration` |
| المنطق | `AppViewModel.kt` | كل عملية مالية/مخزونية داخل `withTransaction` واحدة + حاجز `AtomicBoolean` ضد الضغطات المزدوجة + طابع زمني متزايد `freshNow` |
| الواجهة | `ui/screens/*` | توزيع، مخزون، زبائن، تقارير، سجل مبيعات، محطة، إعدادات، قفل رمز |

**جوهر المنطق المحفوظ:** الرصيد الموثوق يُشتق من المعاملات لا من الكاش؛ الإلغاءات محروسة بمطابقة دقيقة؛ لا فقدان بيانات صامت.

---

## 2️⃣ إعادة بناء الواجهات (UI/UX)

بُنيت نسخة ويب كاملة من التطبيق (مجلد `web/`) بهوية بصرية جديدة:
- **لوحة ألوان دلالية ثابتة:** لهب برتقالي (أساسي) على فحمي دافئ وورقي مطفأ؛ أخضر للمقبوض، برتقالي كهرماني للآجل، قرمزي للأخطاء.
- **شريط تنقل سفلي** بمؤشر منزلق متحرك + انتقالات ناعمة بين التبويبات.
- **مقياس سائل حي** للمخزون بموجتين متحركتين، **دونات متحرك** للمقبوض/الآجل، أرقام تصعد بدل القفز، هياكل عظمية متلألئة تميّز التحميل عن الفراغ.
- **كونفيتي لأول بيع في اليوم** (الاستثناء الاحتفالي الوحيد وفق دليل الأنيميشن §5.3).
- **تفضيل «تقليل الحركة»** يوقف كل التحريكات + احترام `prefers-reduced-motion`.

**التحريكات موضّحة بالصورة المتحركة في `changes.html`** (مُصمم لشاشات الهاتف فقط).

## 3️⃣ تحسين المنطق ومعالجة الأخطاء

### أخطاء/ثغرات أُصلحت في نسخة الويب أثناء البناء
| # | المشكلة | الإصلاح |
|---|---|---|
| 1 | `COUNT(*)` يصل نصاً من PostgreSQL فكان «أول بيع اليوم» يُكشف خطأً (`"0" === 0` فاشلة) | تحويل صريح `Number()` قبل المقارنة |
| 2 | منتصف الليل كان يُحسب بتوقيت الخادم لا بتوقيت المستخدم | حساب ثابت على توقيت عدن (UTC+3 بلا صيفي) — إصلاح الفحص L17 |
| 3 | حاجز العمليات `AtomicBoolean` في الذاكرة لا يكفي مع تعدد الخوادم | قفل صفوف `FOR UPDATE` داخل معاملة قاعدة البيانات — ذرّي حقيقياً |

### القواعد المحفوظة بحذافيرها من تطبيق Kotlin
- منع البيع فوق المخزون برسالة تُبيّن المتوفر.
- منع إلغاء بيع له تحصيلات لاحقة + رسالة قابلة للتصرف (عدد/مجموع/أقدم دفعة).
- مطابقة `soldDate` عند استرجاع الأسطوانات — أسطوانة أُعيد بيعها لا تُمس.
- إلغاء سحب المحطة فقط إن كانت أسطواناته لم تُبَع (بالربط الصريح `purchaseId`).
- منع حذف زبون له عمليات، منع تكرار الأسماء (بلا حساسية لحالة الأحرف).
- المدفوع عند البيع/السحب ضمن `[0, الإجمالي]` — السالب مرفوض دائماً.
- السداد الزائد يتحول رصيداً دائناً لصالح الزبون (إصلاح الفحص 7).
- السعر 0 مسموح (منحة/عينة) والسالب مرفوض (إصلاح الفحص 82).
- رمز قفل مملّح + سلّم تصاعدي: 60ث ← 5د ← 30د ← ساعتان كل 4 محاولات.
- أسقف المبالغ: مليار ريال، والكميات 10,000 — ضد التشبع و`OOM`.

## 4️⃣ الفحص اليدوي (دوال ميتة، استيرادات، اتساق)

| الفحص | النتيجة |
|---|---|
| دوال غير مستخدمة في نسخة الويب | حُذفت (`isFirstSaleToday` المكررة، مكوّن `Skeleton` غير المستخدم) |
| استيرادات ميتة | حُذف استيراد `like` غير المستخدم |
| ملاحظات على كود Kotlin | الكود محصَّن أصلاً بتعليقات إصلاحات سابقة؛ لا دوال ميتة فيه، وكل الدوال تخدم المنطق. ملاحظة يتيمة: `CylinderDao.getSoldCount`/`getAvailableCount` تُستخدمان في `refreshInventory` ✓، و`Money.piastersToPounds(Double)` مخصصة للعدادات المتحركة ✓ |
| اختبارات نهاية-طرف عبر الـ API | 21 سيناريو: بيع بلا مخزون (رُفض)، تحصيل بعد البيع ثم محاولة إلغاء (رُفض برسالة مفصلة)، عكس التحصيل ثم إلغاء (نجح وأعادت المخزون)، تسديد محطة أكبر من الدين (رُفض)، تكرار اسم زبون (رُفض)، سداد زائد → رصيد −5000 ✓، قفل الرمز بعد 4 محاولات ✓ |

## 5️⃣ الفحص العكسي (كيف يمكن أن يفشل؟)

- **ضغطات مزدوجة سريعة** → حاجز واجهة + قفل صفوف في القاعدة معاً.
- **نسخة احتياطية/استيراد** → بقيت في تطبيق Kotlin (فحص سلامة + إصدار + جداول + تحقيق Room)؛ نسخة الويب تعتمد قاعدة بيانات خادمية فلا ملف يُستورد.
- **تعارض المنطقة الزمنية** → أُمّن بحساب عدن الثابت.

## 6️⃣ محتوى هذه الدفعة المرفوعة

```
تغييرات هذه الجلسة:
├── تقرير_الفحص_والتحسينات.md   ← هذا الملف
├── changes.html                 ← عرض متحرك «قبل/بعد» (شاشات هاتف فقط)
└── web/                         ← نسخة الويب المعاد بناؤها (Next.js + PostgreSQL)
    ├── src/                     ← الواجهات + منطق الأعمال + مخطط قاعدة البيانات
    └── README.md                ← طريقة التشغيل
```

> التطبيق الأصلي (Kotlin) لم يُمَسَّ — ملفات `app/` كما هي. نسخة الويب موازية تحفظ جوهر المنطق نفسه.
